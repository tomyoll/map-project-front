import React from 'react';

export const MarkersContext = React.createContext(null);
export const SelectedMarkerContext = React.createContext(null);
export const VisibleMarkersContext = React.createContext(null);
